// -------------------------------------------------- //
// This file is autogenerated by pioasm; do not edit! //
// -------------------------------------------------- //

#pragma once

#if !PICO_NO_HARDWARE
#include "hardware/pio.h"
#endif

// ------------ //
// ws2812_multi //
// ------------ //

#define ws2812_multi_wrap_target 0
#define ws2812_multi_wrap 3

#define ws2812_multi_T1 2
#define ws2812_multi_T2 5
#define ws2812_multi_T3 3

static const uint16_t ws2812_multi_program_instructions[] = {
            //     .wrap_target
    0x6021, //  0: out    x, 1                       
    0xa10b, //  1: mov    pins, !null            [1] 
    0xa401, //  2: mov    pins, x                [4] 
    0xa203, //  3: mov    pins, null             [2] 
            //     .wrap
};

#if !PICO_NO_HARDWARE
static const struct pio_program ws2812_multi_program = {
    .instructions = ws2812_multi_program_instructions,
    .length = 4,
    .origin = -1,
};

static inline pio_sm_config ws2812_multi_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + ws2812_multi_wrap_target, offset + ws2812_multi_wrap);
    return c;
}

#include "hardware/clocks.h"
void ws2821_multi_program_init(PIO pio, uint sm, uint offset, uint pin, float freq) {
    pio_gpio_init(pio, pin);
    pio_sm_set_consecutive_pindirs(pio, sm, pin, 1, true);
    pio_sm_config c = ws2812_multi_program_get_default_config(offset);
    sm_config_set_sideset_pins(&c, pin);
    sm_config_set_out_shift(&c, false, true, 24);
    sm_config_set_fifo_join(&c, PIO_FIFO_JOIN_TX);
    int cycles_per_bit = ws2812_multi_T1 + ws2812_multi_T2 + ws2812_multi_T3;
    float div = clock_get_hz(clk_sys) / (freq * cycles_per_bit);
    sm_config_set_clkdiv(&c, div);
    pio_sm_init(pio, sm, offset, &c);
    pio_sm_set_enabled(pio, sm, true);
}

#endif

